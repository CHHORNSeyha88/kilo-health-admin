spring:
  application:
    name: Only-Admin
  datasource:
    url: jdbc:mysql://localhost:${PORT}/only_admin
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

    driver-class-name: com.mysql.cj.jdbc.Driver
    spring:

    # Mail server configuration
  mail:
    username: chhanratta92000@gmail.com
    password: ablu ngch ukta susu
    host: smtp.gmail.com
    port: 587
    properties:
      mail:
        smtp: 
          auth: true
          starttls: 
            enable: true
    debug: true
    
  flyway:
    url: jdbc:mysql://localhost:${PORT}/only_admin
    schemas: only_admin
    user: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          batch_size: 100
        order_updates: true
        order_inserts: true
